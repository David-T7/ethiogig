EMAIL_HOST=smtp-relay.brevo.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=

# Create at https://aistudio.google.com/apikey — never commit the real value
GEMINI_API_KEY=

# Get from https://dashboard.chapa.co — never commit the real value
CHAPA_SECRET_KEY=

# Dedicated signing key for candidate magic-link tokens (password/email change).
# Must differ from SECRET_KEY in production. Generate with:
#   python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
CANDIDATE_ACTION_SECRET_KEY=
